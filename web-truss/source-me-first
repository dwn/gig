if [ "$VIRTUAL_ENV" == "" ]; then
  rm -rf .env
  virtualenv .env
  deactivate
  source .env/bin/activate
  pip install -r requirements.txt
  heroku plugins:install heroku-repo
  heroku repo:purge_cache -a skillguild
  heroku config:set HEROKU=True
fi
